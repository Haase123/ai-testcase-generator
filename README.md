## Optional AI Integration

The project supports optional OpenAI integration for enhanced natural-language test case generation.

To enable it, add .env with:

OPENAI_API_KEY=your_api_key


If no API key or credits are available, the application automatically falls back to the built-in rule-based test generation engine.
